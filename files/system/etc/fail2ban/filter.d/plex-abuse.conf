[Definition]
# Matches abuse/scanning attempts on Plex endpoints via Caddy
# Detects 4xx errors on plex.* subdomain requests
failregex = ^.*"remote_ip":"<HOST>".*"host":"plex\.[^"]*".*"status":4\d\d\b.*$
ignoreregex =
